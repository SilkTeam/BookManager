@{
    ViewBag.Title = "Book";
    Layout = "~/Views/Shared/_Manager.cshtml";
    var list = ViewBag.list as List<BookManager.Models.Book>;
}
<h2>书籍管理</h2>
<table class="table table-hover">
    <thead>
        <tr>
            <th>编号</th>
            <th>封面</th>
            <th>书名</th>
            <th>介绍</th>
            <th>分类</th>
            <th>作者</th>
            <th>数量</th>
            <th>操作</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in list)
        {
            <tr>
                <td>@item.ID</td>
                <td>@item.Img</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Category.Name</td>
                <td>@item.Author.</td>
                <td>@item.TID</td>
                <td>
                    <div class="btn-group">
                        <a href="/Manager/EditBook?ID=@item.ID" class="btn btn-warning" role="button">编辑商品</a>
                        <a href="#DelShop" id="DelShop" onclick="DelShop(@item.ID)" class="btn btn-danger" role="button">删除商品</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="bd-example">
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            @if (ViewBag.page == 1)
            {
                <li class="page-item disabled">
                    <a class="page-link" href="#" aria-disabled="true">首页</a>
                </li>
            }
            else
            {
                <li class="page-item">
                    <a class="page-link" href="/Home/Index?page=1">首页</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="/Home/Index?page=@(ViewBag.page - 1)">上页</a>
                </li>
            }
            <li class="page-item"><a class="page-link" href="/Home/Index?page=@(ViewBag.page)">@(ViewBag.page)</a></li>
            @if (ViewBag.page == ViewBag.pageCount)
            {
                <li class="page-item disabled">
                    <a class="page-link" href="#" aria-disabled="true">尾页</a>
                </li>
            }
            else
            {
                <li class="page-item">
                    <a class="page-link" href="/Home/Index?page=@(ViewBag.page + 1)">下页</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="/Home/Index?page=@(ViewBag.pageCount)">尾页</a>
                </li>
            }
            <div class="col-3 btn-group">
                <input type="number" id="PageJ" class="form-control col-6" placeholder="请输入页数">
                <a href="#junpPage" id="junpPage" class="btn btn-info col-4" role="button">确定</a>
            </div>
        </ul>
    </nav>
</div>
@section scripts{
    <script>
        $("#junpPage").click(function () {
            layer.load(2, { shade: false });
            setTimeout(function () {
                window.location.href = "/Home/Index?page=" + $("#PageJ").val();
            }, 350);
            return false;
        });
    </script>
}